# pnpm 安装策略。
#
# 1) `node-linker=hoisted`：扁平化 node_modules（真实目录，而非符号链接/联接）。
# 2) `package-import-method=copy`：从内容寻址存储复制文件而非硬链接。
#
# 原因：部分沙箱/受限环境（含本机构建机）会静默拒绝创建符号链接、联接与硬链接，
# 表现为 `node_modules/<pkg>` 为空目录、`.bin` 垫片断链（MODULE_NOT_FOUND）。
# 复制 + 扁平化组合可稳定规避；在普通开发机上两种模式行为一致。
node-linker=hoisted
package-import-method=copy
