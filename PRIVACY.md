# 隐私声明（Privacy Statement）· DesktopPet v0.1.0

生效日期：2026-09-15

本声明说明「桌面宠物 DesktopPet」如何对待您的数据。**一句话结论：完全离线、零采集、数据只在本机。**

## 一、数据收集

本软件**不收集、不存储、不对外传输任何个人数据**，包括但不限于：

- 不采集身份信息（姓名、账号、设备标识、IP 地址）；
- 不采集使用行为（点击、停留、偏好画像）；
- 不采集系统信息（硬件、操作系统、已安装软件）。

## 二、网络行为

- 运行期**零网络连接**：不使用任何 HTTP/WebSocket 对外通信能力（capability 已全部关闭、
  CSP 仅允许 `'self'` 与本地 IPC），WebView2 通过 `additionalBrowserArgs` 显式禁用遥测与
  后台网络（`--disable-background-networking --disable-component-update --disable-sync
  --disable-domain-reliability`）。
- 构建期唯一联网点：制作「离线标准安装包」时，打包工具需一次联网下载 Microsoft 官方
  WebView2 离线运行时（内嵌进安装包，此后安装全程无需联网）。

## 三、数据存储与本地性

- 存档、设置、日志仅写入本机应用数据目录 `%APPDATA%\com.desktoppet.app`；
- 卸载软件时**默认保留存档**（卸载器「删除应用数据」复选框默认不勾选）；
- 存档损坏时，软件会隔离损坏档并自动重建默认档，原档保留在本地可回捞。

## 四、权限

本软件不使用麦克风、摄像头、位置、通讯录等敏感权限。系统级能力（窗口置顶、
穿透点击、托盘、全局键鼠钩子）仅用于桌面宠物自身功能，且均可随时退出/卸载。

## 五、变更

若本声明发生变化，将通过随包文档更新；本软件无在线推送能力。

## 六、联系

如对本声明有疑问，请通过项目仓库 issue 反馈。
