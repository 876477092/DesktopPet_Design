//! `dp-audio`：音频播放（T-01 占位 crate）。
//!
//! 承载内容（`02 §3`）：`player`（解码 + 播放）、`bus`（事件总线接入）。
//!
//! 契约要点（`02 §1.4` / `§7.7`）：
//!   - 运行于独立 `std::thread`，队列上限 8，溢出丢弃（不重试、不阻塞 core-loop）；
//!   - 音效 OGG ≤200KB/条，采样率 44.1kHz，统一归一化 −16 LUFS；
//!   - 资源来自只读资源目录，禁止任何网络拉取（C9）。
