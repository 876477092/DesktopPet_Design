[package]
name = "dp-activity"
description = "外出活动：岗位 / 课程 / 旅游的状态机、结算、明信片与异常（含 DST）"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lib]
name = "dp_activity"
path = "src/lib.rs"

# S8-M1（2026-09-13 补登 P3-1）：空 crate 补声明 `dp-core`（依赖按接口实际使用增补；
# 若 S7-M1 时段扩展致接口漂移，以本卡开工时点接口为准）。
[dependencies]
dp-core = { path = "../dp-core" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
