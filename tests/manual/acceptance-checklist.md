# 心月狐 · Stage 10 全量验收清单（S10-M3 · T-26）

> 本清单是发布前的**最终人工验收**入口。自动化部分（`cargo test --workspace`、
> `pnpm exec vitest run`、`pnpm build`、`scripts/check-mem.ps1`）已在回归中跑过，
> 结论见同目录《Stage10-回归报告.md》；本文件只列**必须在真实桌面/真实宠物窗口
> 手点**的项。每项由验收人勾选并填实测值。

运行版本：`DesktopPet.exe`（release）· 日期：____ · 验收人：____

---

## A. 回归门禁（自动化，须全绿）

| # | 命令 | 通过标准 | 结果 |
|---|------|----------|------|
| A1 | `cargo check --workspace` | 0 error / 0 warning（新代码） | ☐ |
| A2 | `cargo test --workspace` | 全部测试 `0 failed` | ☐ |
| A3 | `pnpm exec vitest run` | 全部测试 `0 failed` | ☐ |
| A4 | `pnpm typecheck` | tsc 0 error | ☐ |
| A5 | `pnpm build` | vite 产物生成、settings/pet 双 HTML | ☐ |
| A6 | `scripts/check-mem.ps1` | 空闲 ≤3% 且峰值 ≤250MB（status≠fail） | ☐ |

---

## B. S10-M1 · 增量 UI 面板

### B1 属性 Tab（六维实时值，FR-12.6）
- [ ] 打开设置 →「属性」：六条属性条（心情/体力/饱食/清洁/亲密度/无聊）实时刷新。
- [ ] 亲密度显示 `Lv·exp/next`（满级显示 `Lv·满级`），不硬编码数值。
- [ ] 数值随交互变化（喂饭→饱食↑，抚摸→心情/亲密↑），约 1s 内更新。

### B2 活动 Tab（FR-13）
- [ ] 空闲时列出 打工/学习/旅游 三类派遣按钮（文案来自 `activities.json`）。
- [ ] 点「打工 · 30min」→ 活动卡进入进行态：进度条 + 剩余时间倒计时。
- [ ] 进行中「提前召回」可用；空闲时不显示召回钮。
- [ ] 体力/饱洁不足或安静时段派遣被拒时，**不弹窗**，桌面给反馈、设置页不崩。

### B3 商城 + 背包 Tab（FR-14）
- [ ] 顶部显示心币余额，随打工入账刷新。
- [ ] 30 件商品按目录渲染；余额不足时购买钮置灰。
- [ ] 购买一件食物 → 背包列表 `×n` 增加；余额正确扣减；失败时余额回滚（不双扣）。

### B4 相册 + 桌面装饰（FR-13-6）
- [ ] 相册网格渲染照片（无照片时显示空态文案，不崩）。
- [ ] 已拥有的「摆件」类商品出现在装饰选品列。
- [ ] 选中摆件 → 点空槽放入；点已占槽取下。
- [ ] 装饰 **恒 5 槽**；槽位越界 / 背包无该摆件时拒绝写入。
- [ ] 摆放结果重启后保留（`save.decor` 落盘）。

---

## C. S10-M2 · 情绪可解释性（FR-12.6）

- [ ] 进入委屈/生气（冷落累积）后，**长按桌面心情条** ≥500ms 弹出原因卡。
- [ ] 原因卡显示冷落压力 `P/cap` 进度、当前档位 `L0~L5`。
- [ ] 按影响权重列出主因（如「你不在 / 太久没理我」），每条带方向（加速/缓解）与一句建议。
- [ ] 情绪转好后长按显示「现在情绪不错」空态；关闭钮可关。
- [ ] 旅游返回时明信片在相册可见（明信片挂件可拖、可关）。

---

## D. 发布前体检（通用红线）

- [ ] 全新安装首启：默认档正常，无崩溃、无红色报错。
- [ ] 杀进程后重开：上次进度/装饰/背包保留（落盘正确）。
- [ ] 多开被单实例拦截；托盘菜单 显示/隐藏/退出 正常。
- [ ] 日志 `dropped` 计数为 0（入站指令无堆积丢弃）。
- [ ] 中文/英文切换：新 Tab 与原因卡文案齐全，无裸 key 漏译。
- [ ] 长时间挂起（>10min）无内存爬升、无存档损坏告警。

---

**总评**：☐ 通过，可发布　　☐ 有遗留缺陷（单号：____）
